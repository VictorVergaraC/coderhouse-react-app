import React from 'react';

const Layout = ({ children }) => {
    return (
        <div>
            <h2>
                Componente Layout
            </h2>
            { children }
        </div>
    );
}

export default Layout;
