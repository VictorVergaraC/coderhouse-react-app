const Hijo = ({ mensaje, mesada }) => {
    return (
        <>
            <div>Component hijo</div>
            <p>Mensaje: { mensaje }</p>
            <p>Mesada: ${ mesada }</p>
        </>
    );
}

export default Hijo;
